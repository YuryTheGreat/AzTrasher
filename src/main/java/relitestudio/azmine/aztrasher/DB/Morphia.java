package relitestudio.azmine.aztrasher.DB;

import com.mongodb.client.MongoClient;

public class Morphia {
    private MongoClient mc;
    private Morphia morphia;

    public Morphia(int i) {
        //mc = new MongoClient();
        //morphia = new Morphia();
    }
}
